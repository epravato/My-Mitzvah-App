{
  "name": "tefillinchallenge-api",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute tefillinchallenge-db --remote --file=./schema.sql",
    "db:init:local": "wrangler d1 execute tefillinchallenge-db --local --file=./schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.118.0"
  },
  "dependencies": {
    "worker-mailer": "^1.2.1"
  }
}
